<div class="container">
    <h1><%= t('collections.add') %></h1>

    <div class="alert alert-info">
        <p>
            Una colección es un conjunto de objetos con algunos rasgos en común. 
            Pueden pertenecer a una misma época, a un mismo autor, tener la misma función, etc.
        </p>
        <p>
            Puedes añadir tantos objetos como desees a la colección haciendo 
            click en el enlace "Añadir objeto" y seleccionando el objeto que desees. 
            Opcionalmente, puedes asignar una <b>imagen de previsualización</b> a tu colección. 
            Esta imagen se mostrará en la galería y en tu perfil.
        </p>
    </div>

    <%= form_for(@collection, :html => { multipart: true }) do |f| %>

        <%= f.label :title %><br/>
        <%= f.text_field :title, class: 'form-control' %>
        <br/>
        <%= f.label :description %><br/>
        <%= f.text_area :description, class: 'form-control' %>
        <br/>
        <%= f.label :avatar %>
        <%= f.file_field :avatar %><br/>
        <br/>

        <div id="collections_restored_objects field">
            <h2><%= t('collections.restored_objects')%></h2>
            <br/>
            <div class="links">
                <%= link_to_add_association f, :collections_restored_objects do %> <%= t('collections.add_restored_objects') %> <% end %>
            </div>
        </div>
        <br/>
        <%= f.submit t('collections.save'), class:'btn btn-primary' %>
    <% end %>
</div>